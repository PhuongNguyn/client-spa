

const HorizontalDivider = ({width}: {width: string}) => {
    return (
        <div className="horizontal-divider">
            <div className="horizontal-divider-line" style={{width}}>
                
            </div>
        </div>
    )
}

export default HorizontalDivider